{% extends 'base.html.twig' %}

{% block title %}{{ 'comment.edit.title'|trans }}{% endblock %}

{% block body %}
    <h1>{{ 'comment.edit.title'|trans }}</h1>

    {{ include('comment/_form.html.twig', {'button_label': 'Update'}) }}

    {% if is_granted('ROLE_ADMIN')%}
    <a class="btn btn-primary" href="{{ path('app_comment_index') }}">{{ 'back to list'|trans }}</a>
    {% else %}
    <a class="btn btn-primary" href="{{ path('app_user_index') }}">{{ 'back to list'|trans }}</a>
    {% endif %}


    {{ include('comment/_delete_form.html.twig') }}
{% endblock %}
