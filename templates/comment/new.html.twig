{% extends 'base.html.twig' %}

{% block title %}{{ 'comment.new.title'|trans }}{% endblock %}

{% block body %}
    <h1>{{ 'comment.new.title'|trans }}</h1>

    {{ include('comment/_form.html.twig') }}

    <a href="{{ path('app_comment_index') }}">{{ 'back to list'|trans }}</a>
{% endblock %}
